<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.announcement.AnnouncementFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/announce_recyceler"
        android:layout_width="match_parent"
        android:layout_height="520dp"
        tools:layout_editor_absoluteX="-6dp"
        tools:ignore="MissingConstraints" />

    <EditText
        android:id="@+id/announceText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        app:layout_constraintBottom_toBottomOf="parent" />

    <Button
        android:id="@+id/AsendButton"
        android:layout_width="60dp"
        android:layout_height="wrap_content"
        android:background="@android:drawable/ic_media_play"
        android:backgroundTint="#566EB6"
        app:backgroundTint="#566EB6"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/announceText" />
</androidx.constraintlayout.widget.ConstraintLayout>